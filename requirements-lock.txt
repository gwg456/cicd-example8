# 锁定版本的依赖文件
# 这个文件包含精确的版本号，用于确保环境一致性
# 生成方式: pip freeze > requirements-lock.txt
# 使用方式: pip install -r requirements-lock.txt

# 核心依赖（精确版本）
prefect==3.4.8
prefect-docker==0.6.6
httpx==0.24.1
python-dotenv==1.0.1
pydantic==2.5.0
structlog==23.2.0
rich==13.7.0

# 传递依赖（主要的）
anyio==4.2.0
certifi==2023.11.17
click==8.1.7
cloudpickle==3.0.0
croniter==2.0.1
fastapi==0.104.1
h11==0.14.0
httpcore==1.0.2
idna==3.6
jinja2==3.1.2
markupsafe==2.1.3
packaging==23.2
pendulum==3.0.0
pydantic-core==2.14.5
python-dateutil==2.8.2
python-multipart==0.0.6
pyyaml==6.0.1
six==1.16.0
sniffio==1.3.0
sqlalchemy==2.0.23
starlette==0.27.0
typer==0.9.0
typing-extensions==4.9.0
uvicorn==0.24.0
websockets==12.0

# 注意：
# 1. 这个文件应该定期更新以获取安全补丁
# 2. 在生产环境中使用此文件确保版本一致性
# 3. 开发时可以使用 requirements.txt 或 pyproject.toml
# 4. 更新依赖时请先在测试环境验证兼容性
